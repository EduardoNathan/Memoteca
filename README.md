# 🧠 Memoteca

Projeto desenvolvido para aprender **protocolos HTTP** e **integração com APIs**.

---

## 📖 Visão geral

**Memoteca** é uma aplicação web que consome uma API backend para armazenar e exibir conteúdos.  
O foco principal do projeto é compreender como funcionam os métodos **HTTP (GET, POST, etc.)**, como o front-end interage com o backend e como integrar ambos de forma prática.

---

## 🚀 Funcionalidades

- Front-end em **HTML**, **CSS** e **JavaScript puro** com consumo de API.  
- Backend simples utilizando **JSON Server** (simulando um banco de dados).  
- Inclusão e listagem de registros via **requisições HTTP**.  
- Ideal para estudos e prática de integração front-back.

---

## 🛠️ Tecnologias utilizadas

- **HTML5**  
- **CSS3**  
- **JavaScript (ES6+)**  
- **JSON Server**  
- **Node.js** (para rodar o backend simulado)

---

## 💻 Como executar localmente

1. **Clone o repositório:**
   ```bash
   git clone https://github.com/EduardoNathan/Memoteca.git

2. **Entre na pasta backend:**
cd Memoteca/backend

3. **Instale as dependências e inicie o JSON Server:**
npm install
npm start
Isso iniciará o backend fake, permitindo que o front-end consuma a API corretamente.

4. **Abra o arquivo index.html no navegador:**
Utilize uma extensão como Live Server para executar localmente.

⚠️ Importante: Para visualizar o site em pleno funcionamento, é necessário rodar o JSON Server (backend) com o comando npm start dentro da pasta /backend.
